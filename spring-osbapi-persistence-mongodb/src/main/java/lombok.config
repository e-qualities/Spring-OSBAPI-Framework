# Lombok Configuration, see https://projectlombok.org/features/configuration

# Adds @lombok.Generated annotations to all generated nodes where possible; useful for JaCoCo (which has built in support),
# or other style checkers and code coverage tools
lombok.addLombokGeneratedAnnotation = true
lombok.equalsAndHashCode.callSuper = call